type User {
  id: ID
  name: String
  password: String
  role: String # Make this an enum type
  phone_number: String
  email: String
  status: String # Make this an enum type
}

type Query {
  users: [User]
}
